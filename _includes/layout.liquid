<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ title }}</title>

    <style>
.content {
  line-height: 1.6;
  margin: 0 auto;
  max-width: 1100px;
}

article {
  display: grid;
  grid-template-columns: 1fr 3fr 1fr;
  grid-gap: 20px;
}

article .images {
  display: grid;
  grid-gap: 10px;
  align-items: end;
  text-align: right;
}

article .images.left {
  grid-column: 1 / 3;
}

article .images.center {
  grid-column: 2 / 3;
}

article img {
  max-width: 100%;
}

article .imagenote {
  grid-column: 1 / 2;
  color: #999;
}

article p, article h1,
article ol, article ul {
  margin-top: 0;
  margin-bottom: 0;
  grid-column: 2 / 3;
}

article .gutternote {
  grid-column: 3 / 4;
  color: #999;
}
    </style>
  </head>
  <body>
    <div class='content'>
      <a href='/'>← home</a>
      <h1>{{ title }}</h1>

      <article>
        {{ content }}
      </article>
    </div>
  </body>
</html>